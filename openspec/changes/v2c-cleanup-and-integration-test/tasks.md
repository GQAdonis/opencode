# Tasks

- [ ] 1. Delete spike evidence tests under openspec/changes/v2-spike-validate-gaps/evidence/ (keep spike.md prose)
- [ ] 2. Add ranking-plugin integration test (load fork.skill-ranking into the plugin host; assert the <available_skills> block is reordered/filtered on a driven request); model on packages/core/test/plugin.test.ts
- [ ] 3. Verify: integration test green; fork-plugins + core suites still pass
