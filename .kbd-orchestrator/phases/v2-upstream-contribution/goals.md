# Goals

- File the session.hook('request') seam PR to anomalyco/opencode (lead PR — 'implement what you documented')
- File the $defs-inlining bug-fix PR (packages/llm)
- File the model-persistence bug-fix PR (packages/app), incl. the async-storage restore race that affects Electron too
- Delete spike evidence tests from openspec/changes/v2-spike-validate-gaps/evidence/ before any merge
- Add an end-to-end integration test for the fork.skill-ranking plugin (currently type-verified only)
